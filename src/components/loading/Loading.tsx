const Loading = () => {
  return (
    <div
      style={{
        position: "absolute",
        left: "50%",
        transform: "translateX(-50%)",
      }}
      className="loading"
    >
      Loading
    </div>
  );
};

export default Loading;
